Public Class MainScreen
    Dim tick As Integer = 0
    Dim maxtick As Integer = 100

    Private Sub paraDrop(sender As Object, e As EventArgs) Handles paraDropTimer.Tick
        tick = tick + 1
        If tick = maxtick And maxtick > 10 Then
            'insert function to drop a parachute
            fillerLabel.Text = maxtick 'filler line
            maxtick = maxtick - 5
            tick = 0
        ElseIf tick = maxtick Then
            'insert function to drop a parachute
            tick = 0
        End If
    End Sub
End Class